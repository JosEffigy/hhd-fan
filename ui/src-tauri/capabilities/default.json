{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the local fan UI",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:allow-listen"]
}
